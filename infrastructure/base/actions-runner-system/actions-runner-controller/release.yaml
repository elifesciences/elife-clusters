---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: actions-runner-system
spec:
  interval: 1m
  chart:
    spec:
      chart: actions-runner-system
      version:
      sourceRef:
        kind: HelmRepository
        name: actions-runner-system
      interval: 1m
  install:
    remediation:
      retries: 5
  values:
    installCRDs: false
    prometheus:
      servicemonitor:
        enabled: false
